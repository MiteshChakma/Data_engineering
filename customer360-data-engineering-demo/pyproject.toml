[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "customer360-demo"
version = "0.1.0"
description = "Local Customer 360 data engineering and ML workflow demo"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
